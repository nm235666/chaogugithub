# Web 前端整改回归清单（2026-04-04）

## 1. 构建与静态检查
- [x] 在 `apps/web` 执行 `npm run build` 成功（2026-04-04 已多轮通过）
- [x] 无新增 TypeScript 编译错误
- [x] 无阻断级别构建告警（体积告警仅记录，不阻断）

## 2. 页面主链路（手动 smoke）
- [ ] 登录页：登录 / 注册 / 找回密码入口可用
- [ ] 导航：桌面导航 + 移动菜单可打开、关闭、跳转
- [ ] 筛选：输入筛选条件后，结果可刷新且分页可用
- [ ] 详情抽屉：可打开、Esc 关闭、焦点回到触发点
- [ ] 高风险动作：删除/停用/强制下线前出现确认提示

## 3. 移动端可读性（重点）
- [ ] 股票列表页：小屏展示为卡片，不依赖表格横向滚动
- [ ] 宏观页：小屏展示为卡片，不依赖表格横向滚动
- [ ] 信号审计页：小屏展示为卡片，不依赖表格横向滚动
- [ ] 信号质量配置页：小屏支持卡片化编辑规则与黑名单

## 4. 可访问性（键盘 + 语义）
- [ ] 可点击卡片可用键盘聚焦与触发
- [ ] 抽屉具备 `role=dialog` / `aria-modal` / 标题关联
- [ ] 图表有文本摘要（读屏器可读）
- [ ] 焦点可见样式清晰

## 5. 可自动化候选
- [ ] E2E：登录后进入股票列表，筛选并翻页
- [ ] E2E：打开任一详情抽屉并验证 Esc 关闭
- [ ] E2E：执行危险操作按钮，断言出现确认框
- [ ] 组件测试：InfoCard 在交互态渲染为 `button`
