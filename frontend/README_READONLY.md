# frontend/ 只读冻结公告

生效时间：2026-03-31 UTC

本目录（旧版 Vanilla HTML/JS 前端）已冻结为只读存档：

- 不再接收功能开发。
- 不再作为默认启动入口。
- 仅用于历史追溯和紧急回滚参考。

当前默认前端入口：

- 目录：`/home/zanbo/zanbotest/apps/web`
- 访问：`/dashboard` 等新版路由
- 启动：`./start_frontend.sh` 或 `./start_all.sh`

如需新增页面或修复前端问题，请在新版 Vue 前端中修改。
